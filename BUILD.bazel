load("@openapi_tools_generator_bazel//:defs.bzl", "openapi_generator")

openapi_generator(
    name = "scopus_search",
    generator = "haskell-http-client",
    spec = "specs/scopus_search.yaml",
)

